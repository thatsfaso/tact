# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V7
.Trashes
ehthumbs.db
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# TACT generated output (STL files are large — users generate their own)
*.stl
examples/*.stl
output/

# API keys (never commit these)
.env
*.env.local
settings.local.json

# Large model files
*.bin
*.gguf
*.ggml

# LaTeX build artifacts (paper/tact.pdf itself is tracked)
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/missfont.log

# Wrangler local state (contains account identifiers)
worker/.wrangler/

# Local working material that must never reach the public repository
.claude/
TACT_mockups_preview/
ad_assets/
paper/tact_it.tex
paper/tact_it.pdf
